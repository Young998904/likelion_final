# BEJV21 — 백엔드 자바 심화 프로젝트 워크스페이스

> 이 저장소(BEJV21)는 **여러 개의 독립 프로젝트**를 담는 워크스페이스입니다.
> 각 프로젝트는 자기 폴더 안에 자체 `CLAUDE.md`·빌드 설정·소스를 가집니다.
> 특정 프로젝트를 작업할 때는 **그 폴더의 `CLAUDE.md`가 이 파일과 함께 로드**됩니다.

## 폴더 구조
```
BEJV21/                     ← git 저장소 루트(워크스페이스)
├── CLAUDE.md               ← (이 파일) 워크스페이스 공통 안내
├── as-management/          ← 프로젝트 1: 통합 A/S 접수·처리 시스템 (완료)
├── project2/               ← 프로젝트 2 (예정)
├── project3/               ← 프로젝트 3 (예정)
├── (참여기업)...안내.pdf     ← 3개 프로젝트 공통 과제 안내 (gitignore)
└── 제출 결과물.png           ← 참고 자료 (gitignore)
```

## 프로젝트별 규칙은 각 폴더에서
- **as-management** — Java 17 / Spring Boot 3.x, JPA, H2, Thymeleaf 기반 A/S 시스템.
  상세 규칙·설계는 [as-management/CLAUDE.md](as-management/CLAUDE.md) 참조.
- **project2 / project3** — 새 프로젝트를 시작할 때 각 폴더에 전용 `CLAUDE.md`를 만들고
  기술 스택·컨벤션을 그 안에 정의합니다. (이 워크스페이스 파일에는 프로젝트 전용 규칙을 넣지 않습니다.)

## 공통 작업 규칙
- 코드 변경 시 `main` 브랜치에 커밋·push (브랜치 분리하지 않음).
- 주석·UI 텍스트는 한글(관용 영어는 허용), 코드 식별자는 영어.
