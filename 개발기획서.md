# 통합 A/S 접수 및 처리 시스템 — 개발 기획서

> 멋쟁이사자처럼 백엔드 자바 심화 프로젝트 (참여기업 과제 4번)
> 개인 프로젝트 / 경량 버전 · 배포 미포함 · 작성일 2026-07-05

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 과제 | 4. 고객의 요청 및 수리 처리 내역을 관리하는 통합 A/S 접수 및 처리 시스템 |
| 목적 | A/S 센터의 고객 요청·수리 처리 내역을 전산화하여 누락 방지, 담당자 분배, 처리 추적, 입금 확인을 일관되게 처리 |
| 주 사용자 | 내부 직원(A/S 기사, 접수 담당자), 최고관리자, **고객(포털)** |
| 진행 형태 | 원래 팀 과제 → **개인 프로젝트로 경량화** 진행 |
| 성격 | **사내 관리 시스템 + 고객 포털**(직원=계정 로그인 / 고객=전화번호+PIN 세션). 2차 확장으로 고객 접수·결제·조회 추가(§13) |

### 1.1 경량화 방침

- **배포(Deploy)는 진행하지 않음.** Jenkins / Docker / DockerHub / K8s 등 DevOps 스택은 문서상 기술 스택으로만 언급하고 실제 구축 제외.
- 외부 API 연동 중 일부는 **프레임워크 기본 범위 내에서 대체 구현**한다.
  - SMS 발송(접수·입금·배송 알림) → **화면 알림 로그(이력 테이블)로 대체**
  - PG 결제 → **PortOne(포트원) 테스트 연동을 시도하고, 미설정/오류 시 Mock 결제로 폴백**(§13). 실제 결제는 진행하지 않음.
- 목표: **필수 제출물(결과보고서 PPT · 시연영상)을 채울 수 있는 동작 화면**을 완성하는 것.

---

## 2. 제출물 요건 (사진 기준)

| 구분 | 제출물 | 필수 여부 | 본 프로젝트 대응 |
|---|---|---|---|
| 결과보고서 | 팀별 프로젝트 보고서(PPT) | **필수** | 화면 캡처 + 기능 설명으로 작성 |
| 시연영상 | 구조·기능 설명 영상 (5~10분, 파일당 100MB 이하) | **필수(대표)** | 전체 업무 흐름 시연 녹화 |
| 소스코드 | 압축 풀린 tree 형태 권장 | 선택 | GitHub 또는 tree 형태 제출 |
| 데이터 | 샘플 데이터 | 선택 | 초기 시드 데이터로 대체 가능 |
| 접속링크 | 배포 환경 링크 | 선택 | **미제출**(배포 안 함) |

> 필수 2종(보고서·시연영상)은 **로컬 실행 화면만으로 충족 가능**하므로 배포 없이 제출 조건을 만족한다.

---

## 3. 기술 스택 (확정)

| 영역 | 명세 요구 | 본 프로젝트 채택 | 비고 |
|---|---|---|---|
| 프론트엔드 | React SPA + HTML/CSS + 일부 jQuery | **Thymeleaf 서버렌더링 + 일부 jQuery/Ajax** | 개인·경량 목표에 맞춰 단일 프로젝트로 단순화. 시연/캡처 결과물은 동일 |
| 백엔드 | Spring Framework + Spring Boot | **Spring Boot 3.x (Java 17)** | |
| ORM | JPA + MyBatis 병행 | **Spring Data JPA 단독** | 경량화를 위해 JPA 단독. MyBatis 미사용 |
| DB | MySQL | **MySQL 8.x (로컬 설치)** | ERD 기반 설계 |
| 인증 | (내부 계정 로그인) | **직원: Spring Security 세션** / **고객: 전화번호+PIN 경량 세션** | 두 인증 realm 분리 |
| 결제 | (PG 연동) | **PortOne 테스트 연동 → 실패 시 Mock 폴백** | 실제 결제 없음, dev 확인용 |
| 부가 라이브러리 | — | 다음 우편번호(임베드), Chart.js(대시보드), PortOne SDK | 무료·테스트키 |
| 형상관리 | GitHub | GitHub | |
| DevOps | Jenkins/Docker/K8s | **문서 언급만, 미구축** | 배포 제외 |

### 3.1 채택하지 않은 명세 항목 정리 (보고서에 근거 명시용)

| 명세 항목 | 처리 | 사유 |
|---|---|---|
| React SPA | Thymeleaf로 대체 | 개인 프로젝트 경량화, 결과물 동일 |
| MyBatis 병행 | 미사용(JPA 단독) | 경량화 |
| MongoDB(비정형) | 미사용 | Appendix 풀스택 기준 항목(선택) |
| SMS 발송 | 알림 로그로 대체 | 외부 API(비용·계약) |
| PG 결제 | **PortOne 테스트 연동 + Mock 폴백으로 포함(2차 확장)** | dev 확인용, 실제 결제 없음 |
| 다음 주소 API | **유지** | 클라이언트 임베드로 키·비용 불필요 |
| 엑셀 일괄등록 | **제외** | 범위 최소화(고객 직접 입력으로 대체) |
| Jenkins/Docker/K8s | 미구축 | 배포 미진행 |

---

## 4. 기능 범위 (명세 대비 구현 계획)

### 4.1 공통

| 기능 | 구현 | 상태 |
|---|---|---|
| 로그인 | 내부 계정 기반 세션 로그인 (회원가입 없음) | 구현 |
| 권한 제어 | 최고관리자(ADMIN) / 일반 직원(STAFF) 역할 분리 | 구현 |
| 계정 관리 | 관리자만 직원 계정 CRUD·정보/아이디 관리 | 구현 |
| 수리내역 프리셋 | 자주 쓰는 수리내역(명칭+금액) 등록·관리 | 구현 |

### 4.2 고객 및 제품 관리

| 기능 | 구현 | 상태 |
|---|---|---|
| 고객 목록 관리 | 고객명·연락처·주소 CRUD | 구현 |
| 엑셀 일괄등록 | ~~Apache POI 업로드/내보내기~~ → 고객 직접 입력 | **제외** |
| 제품 목록 관리 | A/S 가능 제품 등록·수정 | 구현 |
| 자동완성 | 접수 시 고객/제품 필드 Ajax 자동완성 | 구현 |
| 수동입력 | 미등록 데이터 자유 입력(+자동등록 여부 옵션) | 구현 |

### 4.3 A/S 접수 및 처리 흐름

| 기능 | 구현 | 상태 |
|---|---|---|
| 접수 등록 | 고객·제품 선택 + 요청 내용 입력 | 구현 |
| 접수 알림 | ~~SMS~~ → **알림 로그 기록** | 대체 |
| 상태 관리 | 접수 → 입금대기 → 수리중 → 배송대기 → 완료 (선불 상태 전이) | 구현 |
| 담당자 지정 | 내부 계정 기준 담당자 배정 | 구현 |
| A/S 내역 | 수리내역 입력 + 프리셋 선택 | 구현 |
| 주소 검색 | 다음 우편번호 임베드 | 구현 |

### 4.4 금액 산정 및 입금 관리

| 기능 | 구현 | 상태 |
|---|---|---|
| 수리 금액 계산 | 제품·수리항목(프리셋) 기반 자동 합산 | 구현 |
| 입금 요청 | ~~가상계좌 SMS~~ → **알림 로그 기록** | 대체 |
| 가상계좌 연동 | ~~PG 콜백~~ → **수동 "입금확인" 버튼** | 대체 |
| 입금 상태 | 확인 시 상태 자동 전환(입금대기→배송대기) | 구현 |

### 4.5 배송 및 완료 처리

| 기능 | 구현 | 상태 |
|---|---|---|
| 배송 알림 | ~~SMS~~ → **알림 로그 기록** | 대체 |
| 송장번호 입력 | 송장 등록 → 상태 완료 전환 | 구현 |
| A/S 완료 처리 | 최종 완료 처리 | 구현 |

### 4.6 통계 및 대시보드

| 기능 | 구현 | 상태 |
|---|---|---|
| 요약 통계 | 월별 접수 수·완료 수·미입금 건수 | 구현 |
| 필터링 | 제품별·담당자별·상태별 필터 | 구현 |
| 시각화 | Chart.js로 상태별 분포·완료율 차트 | 구현 |

---

## 5. 데이터 모델 (ERD 초안)

| 엔티티 | 주요 컬럼 | 관계 |
|---|---|---|
| `Employee` (직원 계정) | id, username, password, name, role(ADMIN/STAFF), active | 담당자(1) — 접수(N) |
| `Customer` (고객) | id, name, phone, zipcode, address, addressDetail, **pin(enc)** | 고객(1) — 접수(N) |
| `Product` (제품) | id, name, modelCode, note | 제품(1) — 접수(N) |
| `RepairPreset` (수리내역 프리셋) | id, name, price | 프리셋(1) — 접수내역(N) |
| `AsRequest` (A/S 접수) | id, customer_id, product_id, assignee_id, status, requestContent, deliveryZipcode, deliveryAddress, deliveryAddressDetail, trackingNo, courier, totalAmount, paid, **paidAt, paymentRef**, createdAt, completedAt | 접수(1) — 수리내역(N) |
| `AsRepairItem` (수리내역 라인) | id, request_id, preset_id, name, price | 접수(N) — 프리셋(참조) |
| `NotificationLog` (알림 로그) | id, request_id, type(접수/입금/배송), message, createdAt | SMS 대체 이력 |

**상태(status) enum:** `접수 → 입금대기 → 수리중 → 배송대기 → 완료` (선불: 결제 확인 후 수리)

> **2차 확장 필드**: `Customer.pin`(고객 포털 4자리 PIN, BCrypt), `AsRequest.paidAt`(결제 완료 일시)·`paymentRef`(PortOne 거래 식별자, Mock 시 null). 소요시간 분석은 `completedAt − createdAt`으로 별도 필드 없이 계산.

---

## 6. 화면(페이지) 구성

| # | 화면 | 권한 | 주요 요소 |
|---|---|---|---|
| 1 | 로그인 | 전체 | 아이디/비번 |
| 2 | 대시보드 | 전체 | 요약 카드 + Chart.js 차트 + 필터 |
| 3 | A/S 접수 목록 | 전체 | 검색·상태 필터·페이징 |
| 4 | A/S 접수 상세/처리 | 전체 | 상태 전이, 담당자 지정, 수리내역, 입금확인, 송장, 알림 로그 |
| 5 | A/S 신규 접수 | 전체 | 고객/제품 자동완성, 다음 주소, 프리셋 선택 |
| 6 | 고객 관리 | 전체 | CRUD (엑셀 제외) |
| 7 | 제품 관리 | 전체 | CRUD |
| 8 | 수리내역 프리셋 관리 | 전체 | CRUD |
| 9 | 직원 계정 관리 | ADMIN | 계정 CRUD, 권한 지정 |

---

## 7. 프로젝트 구조 (제안)

```
as-management/
├─ src/main/java/com/example/asm/
│  ├─ config/        (Security, 초기 데이터)
│  ├─ domain/        (Entity)
│  ├─ repository/    (JPA Repository)
│  ├─ service/       (비즈니스 로직)
│  ├─ controller/    (페이지 + Ajax API)
│  └─ dto/
├─ src/main/resources/
│  ├─ templates/     (Thymeleaf)
│  ├─ static/        (CSS, JS, jQuery)
│  └─ application.yml
└─ build.gradle
```

---

## 8. 개발 단계 (마일스톤)

| 단계 | 내용 |
|---|---|
| 1. 기반 세팅 | Spring Boot + MySQL 연결, 엔티티·ERD, Security 로그인, 초기 계정 시드 |
| 2. 마스터 데이터 | 고객·제품·프리셋 CRUD, 자동완성 |
| 3. 핵심 흐름 | A/S 접수 등록 → 상태 전이 → 담당자·수리내역·금액 산정 |
| 4. 입금·배송 | 수동 입금확인, 송장 입력·완료 처리, 알림 로그 |
| 5. 대시보드 | 요약 통계·필터·Chart.js 시각화 |
| 6. 마무리 | 시드 데이터 보강, UI 정리, 시연 시나리오 점검, 캡처·녹화 |

---

## 9. 시연 시나리오 (영상용 초안)

1. 관리자 로그인 → 직원 계정 생성 → 권한 차이 확인
2. 고객·제품·프리셋 등록
3. 신규 A/S 접수(고객 자동완성 + 다음 주소 검색)
4. 담당자 지정 + 수리내역(프리셋)으로 견적 산정 → 비용 청구(→ 입금대기)
5. 입금확인(결제/수동) → 수리중 → 수리 완료 → 배송대기, 알림 로그 확인
6. 송장 입력 → 완료 처리
7. 대시보드에서 통계·차트·필터 확인

---

## 10. 구현 우선순위 (범위 최소화 확정)

### 필수 (핵심 흐름 — 반드시 구현)

- 로그인 + 권한분리(ADMIN/STAFF) + 계정관리 화면
- 고객 관리 CRUD / 제품 관리 CRUD / 수리내역 프리셋 CRUD
- A/S 접수 등록 → 상태 관리(접수→입금대기→수리중→배송대기→완료, 선불)
- 담당자 지정 / 수리내역 입력 + 금액 자동산정
- 수동 입금확인 → 송장 입력 → 완료 처리

### 선택 (포함 확정)

- 알림 로그(SMS 대체 이력)
- 대시보드 통계·차트(Chart.js) + 필터
- 자동완성(Ajax) + 다음 우편번호 검색

### 제외

- **엑셀 일괄등록/내보내기** → 고객 직접 입력으로 대체
- 배포(Jenkins/Docker/K8s), MongoDB(비정형), MyBatis, React

### 2차 확장 (확정 · §13)

- 고객 포털: 전화번호+PIN 인증 / 고객 접수(제품 드롭다운) / 본인 접수 조회
- 결제: PortOne 테스트 연동 → 실패 시 Mock 폴백 → 결제 성공 시 자동 입금확인·상태 전이
- 대시보드: 제품별·전체 **평균 처리 소요시간** 분석

> 1차(필수·선택) 기능은 구현·검증 완료 상태. 2차 확장은 위 결정에 따라 진행.

---

## 11. 코딩·표기 컨벤션 (확정)

> 상세·강제 규칙은 [CLAUDE.md](CLAUDE.md) 참조.

- **주석: 무조건 한글** (인라인·Javadoc 전부). '왜'를 설명, 자명한 내용 중복 금지.
- **UI 표기: 기본 한글.** 단 국내에서 영어로 통용되는 관용어(로그인·아이디·비밀번호·이메일·A/S·대시보드 등)는 허용, 억지 번역 금지.
- 코드 식별자(클래스/변수/메서드)는 영어 관례 유지.
- 계층 분리(controller/service/repository/dto), 엔티티 직접 노출 대신 DTO, 트랜잭션 경계는 service.

---

## 12. 확인·결정 완료 사항

- [x] 아키텍처: **Spring Boot + Thymeleaf** (일부 jQuery/Ajax)
- [x] 버전: **Java 17 / Spring Boot 3.x** (상향 안 함)
- [x] DB: **MySQL 로컬 설치**
- [x] ORM: **JPA 단독** (MyBatis 미사용)
- [x] 계정·권한: **ADMIN/STAFF 분리 + 계정관리 화면**
- [x] 선택 기능: **알림 로그 · 대시보드 차트 · 자동완성/다음 우편번호**
- [x] 엑셀 일괄등록/내보내기: **제외**
- [x] SMS: **화면 알림 로그로 대체**
- [x] 배포·MongoDB·MyBatis·React: **범위 제외**
- [x] 컨벤션: **한글 주석 · 한글 UI(관용 영어 예외)**
- [x] **2차 확장(§13)**: 고객 포털(전화+PIN) · 결제(PortOne→Mock 폴백) · 평균 소요시간 분석

---

## 13. 2차 확장 설계 — 고객 포털 · 결제 · 소요시간 분석

> 상세 기능 명세는 [기능명세서.md](기능명세서.md) §12 참조. 여기서는 구현 방향과 최소 변경 원칙을 정리한다.

### 13.1 핵심 재사용 (최소 변경 원칙)
- 관리자 측 업무 흐름·상태머신은 **기존 구현 그대로 사용**한다.
- 고객이 결제하면 결국 **기존 `confirmPayment`**(paid=true + 입금대기→배송대기 원자적 전이)를 호출 → "결제되면 자동으로 다음 단계"가 별도 개발 없이 충족.
- "결제 요청"은 기존 **입금요청 전이(수리중→입금대기)**를 그대로 사용.

### 13.2 고객 인증 (전화번호 + PIN)
- 직원용 Spring Security와 **분리**: `/customer/**`는 permitAll로 두고, 전화+PIN 검증 성공 시 **HTTP 세션에 `customerId` 저장**하는 경량 방식(인터셉터로 세션 확인).
- 최초 접수: 이름·전화·주소 + **PIN 설정**(BCrypt 저장). 재방문: 전화+PIN. **동일 전화는 기존 고객 재사용**(중복 방지).

### 13.3 결제 (PortOne → Mock 폴백)
- `PaymentService` 추상화: PortOne 키가 설정돼 있으면 실 결제창·검증, 없거나 오류면 **Mock 결제**(즉시 성공 처리)로 폴백 → dev에서 키 없이도 흐름 시연 가능.
- 성공 시 `paid=true`, `paidAt`, `paymentRef`(PortOne 거래ID / Mock=null) 기록 후 상태 자동 전이.
- 관리자 **수동 입금확인**도 fallback으로 유지.

### 13.4 소요시간 분석 (F-DASH-04)
- 완료 건의 `completedAt − createdAt` 평균 → **전체 평균 + 제품별 평균**. 추가 필드 없이 기존 데이터로 계산.
- 대시보드에 요약 카드(평균 처리시간) + 제품별 막대 차트 추가.

### 13.5 신규 구성요소 요약
| 구분 | 신규/변경 |
|---|---|
| 엔티티 | `Customer.pin` 추가 / `AsRequest.paidAt`·`paymentRef` 추가 |
| 화면 | 고객 로그인·접수·내 접수 조회·결제(`/customer/**`), 대시보드 소요시간 |
| 서비스 | `CustomerPortalService`, `PaymentService`(PortOne+Mock), 대시보드 소요시간 집계 |
| 보안 | 고객 세션 인터셉터(직원 Security와 분리) |

### 13.6 시연 시나리오 (2차)
1. 고객 포털에서 전화+PIN으로 접수(제품 선택)
2. 관리자: 접수 확인 → 담당자 지정 → 수리내역/금액 입력 → 결제 요청
3. 고객: 포털에서 결제(PortOne 테스트 또는 Mock) → 성공
4. 관리자 화면 상태가 자동으로 배송대기로 전환됨 확인
5. 대시보드에서 제품별 평균 소요시간 확인
